# Deplyoment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: react-microservice-deployment
spec:
  selector:
    matchLabels:
      app: react-microservice-pod
  template:
    metadata:
      labels:
        app: react-microservice-pod
    spec:
      replicas: 1
      containers:
      - name: react-microservice-container
        image: coffeetalkcr.azurecr.io/frontend:v1
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: 80
---
# Service
apiVersion: v1
kind: Service
metadata:
  name: react-microservice-service
spec:
  selector:
    app: react-microservice-pod
  ports:
  - port: 5200
    targetPort: 80
  type: LoadBalancer